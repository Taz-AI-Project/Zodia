import { RequestHandler } from 'express';
import { ServerError } from '../../types/types';
import { ZodiacInfo } from '../models/zodiacModel';

export const zodiacQuery: RequestHandler = async (_req, res, next) => {

}
